<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <title>Art Detail</title>
</head>
<body>
    <div class="header">
        <h1 class="logo-text">My Docent</h1>
    </div>
    
    <div class="container">
        <div class="history">
            <h2>History</h2>
            <ul>
            </ul>
        </div>
        <div class="main">
            <div class="artwork">
                <img src="<%= artwork.art_img_url %>" alt="<%= artwork.art_name %>">
                <div>
                    <p><strong>작품명:</strong> <%= artwork.art_name %></p>
                    <p><strong>작가:</strong> <%= artwork.art_artist %></p>
                </div>
            </div>
            <div class="description">
                <textarea id="description"><%= artwork.art_description %></textarea>
            </div>
            <div id="audioPlayer">
                <audio controls>
                    <source src="<%= artwork.art_audio_url %>" type="audio/mpeg">
                    Your browser does not support the audio element.
                </audio>
            </div>
        </div>
    </div>
</body>
</html>
